services:
  - type: worker
    name: clean-telegram-bot
    env: python
    plan: free
    buildCommand: "echo 'No dependencies needed'"
    startCommand: "python minimal_bot.py"
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PYTHON_VERSION
        value: 3.11
